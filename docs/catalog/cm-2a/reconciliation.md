# Reconciliation

The real preview reconciles 190 source rows: 148 `ready_to_import`, 41 `needs_review`, 1 `missing_media`, and zero in every other class. There are 189 media references. Supplier stock value `50` is explicitly discarded; commercial inventory remains zero.
