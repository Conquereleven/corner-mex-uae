# Export Contract

`cornerops-cornermex-catalog-export-v1` reconciles every source row into exactly one class. It includes stable identity, source SHA/checksum/fingerprint, supplier evidence, AED minor-unit price or blocker, media reference, publication state and zero inventory. Source notes, PII, credentials and supplier secrets are excluded.
