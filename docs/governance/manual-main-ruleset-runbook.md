# Main Ruleset Verification

Active GitHub repository ruleset `CornerMex protected main` (ID `19129376`)
targets `refs/heads/main`. It requires PR review and the checks
`contract-and-migration`, `type-and-runtime` and `schema-authority`; it blocks
deletion, force pushes and bypass.

Verify through Repository Settings > Rules > Rulesets or the GitHub Rules API.
Any discrepancy is a release blocker and must not be silently waived.
